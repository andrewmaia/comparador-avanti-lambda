version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.8
  build:
    commands:
      # trigger the unit tests here
      - echo 'Running unit tests'
      - cd comparador-avanti-lambda
      - npm install
      - npm run test      
